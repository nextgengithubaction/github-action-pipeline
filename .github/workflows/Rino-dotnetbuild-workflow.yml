name: Ci workflow

on:
  push:
    branches:
      - 'feature/rino'


jobs:
  Dotnetbuild:
    runs-on: ubuntu-latest
    steps:   
      
      - name: build
        run: |
          cd ../../
          dotnet build


